<head>
    <link rel="stylesheet" href="/css/style.css">
</head>
<nav>
    <a href="/home">Home</a>
    <a href="/about">About</a>
    <a href="/contact">Contact</a>
    <% if (user.role === 'admin') { %>
      <a href="/notice/new">Post Notice</a>
    <% } %>
    <% if (user.role === 'teacher') { %>
      <a href="/dashboard/teacher">Teacher Dashboard</a>
    <% } %>
    <% if (user.role === 'student') { %>
      <a href="/dashboard/student">Student Dashboard</a>
    <% } %>
    <a href="/notices">Notices</a>
    <a href="/logout">Logout</a>
  </nav>
  <hr>
  
<h2>Log Meeting with <%= student.fullName %></h2>

<form method="POST" action="/meeting/new/<%= student._id %>">
  <label>Date:</label>
  <input type="date" name="date" required><br>

  <label>Topic:</label>
  <input type="text" name="topic" required><br>

  <label>Notes:</label>
  <textarea name="notes"></textarea><br>

  <button type="submit">Save Meeting</button>
</form>
